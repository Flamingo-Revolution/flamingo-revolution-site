{
  "name": "flamingo-revolution-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "assets:generate": "node scripts/generate-assets.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "cf:preview": "pnpm run build && wrangler pages dev",
    "cf:deploy": "pnpm run build && wrangler pages deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.8",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "wrangler": "^4.105.0"
  }
}
