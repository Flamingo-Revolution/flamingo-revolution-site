<div class="flock-scene" aria-hidden="true">
  <div class="flock-scene__layer flock-scene__layer--back"></div>
  <div class="flock-scene__layer flock-scene__layer--front"></div>
  <div class="flock-scene__flamingo">
    <img src="/flock-assets/characters/flamingo-a.svg" class="flock-scene__frame flock-scene__frame--one" alt="" />
    <img src="/flock-assets/characters/flamingo-b.svg" class="flock-scene__frame flock-scene__frame--two" alt="" />
  </div>
</div>

<style>
  .flock-scene {
    position: relative;
    flex: 1 1 auto;
    width: 100%;
    height: 100%;
    min-width: 0;
    min-height: 0;
    overflow: hidden;
    background: #a7ddf3;
    isolation: isolate;
  }

  .flock-scene__layer {
    position: absolute;
    right: -2px;
    bottom: 0;
    left: -2px;
    height: 70%;
    background-repeat: repeat-x;
    background-position: bottom left;
    background-size: auto 100%;
    will-change: background-position;
  }

  .flock-scene__layer--back {
    z-index: 1;
    opacity: 0.68;
    background-image: url('/flock-assets/background/tirana-back.svg');
    animation: flock-pan 24s linear infinite;
  }

  .flock-scene__layer--front {
    z-index: 2;
    background-image: url('/flock-assets/background/tirana-front.svg');
    animation: flock-pan 12s linear infinite;
  }

  .flock-scene__flamingo {
    position: absolute;
    bottom: 2%;
    left: 20%;
    z-index: 4;
    width: 50px;
    height: 72px;
    animation: flock-bounce 0.4s steps(2, end) infinite;
  }

  .flock-scene__frame {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    image-rendering: pixelated;
  }

  .flock-scene__frame--one { animation: flock-frame-one 0.4s steps(1, end) infinite; }
  .flock-scene__frame--two { animation: flock-frame-two 0.4s steps(1, end) infinite; }

  :global(.game-card:hover) .flock-scene__flamingo,
  :global(.game-card:hover) .flock-scene__frame {
    animation-duration: 0.25s;
  }

  :global(.game-card:hover) .flock-scene__layer--back { animation-duration: 16s; }
  :global(.game-card:hover) .flock-scene__layer--front { animation-duration: 8s; }

  @keyframes flock-pan {
    to { background-position-x: -536px; }
  }

  @keyframes flock-bounce {
    50% { transform: translateY(-4px); }
  }

  @keyframes flock-frame-one {
    50% { opacity: 0; }
  }

  @keyframes flock-frame-two {
    0%, 100% { opacity: 0; }
    50% { opacity: 1; }
  }
</style>
