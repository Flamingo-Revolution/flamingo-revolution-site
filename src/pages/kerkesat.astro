---
// /kerkesat/ was merged into /protestat/#kerkesat (both "versioni 1" and
// "versioni 2" now live there, reusing the same siteCopy.hero.demands /
// demandsV2 sources) so the two "protest" pages read as one. Kept as a
// redirect rather than deleted so existing links and bookmarks still land
// somewhere useful.
return Astro.redirect("/protestat/#kerkesat", 301);
---
